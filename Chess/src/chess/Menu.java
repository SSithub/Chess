package chess;

import javafx.scene.shape.Rectangle;

public class Menu extends Rectangle {

    Menu(double size) {
        this.setOpacity(.5);
        this.setWidth(size);
        this.setHeight(size);
    }
}
